import React from "react";
import HeaderInbox from "./HeaderInbox";
import SideBar from "./SideBar";
const InboxPeople = () => {
  return (
    <div className="wrapperInboxPeople">
      <HeaderInbox />
      <SideBar />
    </div>
  );
};

export default InboxPeople;
